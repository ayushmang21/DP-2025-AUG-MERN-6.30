// 1st Type Function
function addNums(num1, num2) {
    sum = num1 + num2
    console.log(sum);
}

addNums(52, 75)
addNums(52, 75)

// 2nd Type Function

const getAvg = function(m1,m2,m3){
    avg = (m1 + m2 + m3)/3;
    return avg;
}

const result = getAvg(52, 75, 89);

console.log(result);
